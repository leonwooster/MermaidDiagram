<Project>
  <!-- Default platform for IDE tooling (Roslyn language server) when no platform is specified -->
  <PropertyGroup Condition="'$(Platform)' == '' OR '$(Platform)' == 'AnyCPU'">
    <Platform>x64</Platform>
  </PropertyGroup>
</Project>
