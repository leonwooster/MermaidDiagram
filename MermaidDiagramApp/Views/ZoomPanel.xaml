<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MermaidDiagramApp.Views.ZoomPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">
        <!-- Toolbar row -->
        <Border Grid.Row="0" Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1" Padding="8">
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Button Content="🔍+" Command="{x:Bind ViewModel.ZoomInCommand}"
                        ToolTipService.ToolTip="Zoom In"/>
                <Button Content="🔍−" Command="{x:Bind ViewModel.ZoomOutCommand}"
                        ToolTipService.ToolTip="Zoom Out"/>
                <TextBlock Text="{x:Bind ViewModel.ZoomLevelDisplay, Mode=OneWay}"
                           VerticalAlignment="Center" Margin="8,0"/>
                <Border Width="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" Margin="4,2"/>
                <Button Content="50%" Click="ZoomPreset_Click" Tag="0.5"
                        Padding="6,4" FontSize="11" ToolTipService.ToolTip="Zoom to 50%"/>
                <Button Content="100%" Click="ZoomPreset_Click" Tag="1.0"
                        Padding="6,4" FontSize="11" ToolTipService.ToolTip="Zoom to 100%"/>
                <Button Content="200%" Click="ZoomPreset_Click" Tag="2.0"
                        Padding="6,4" FontSize="11" ToolTipService.ToolTip="Zoom to 200%"/>
                <Button Content="500%" Click="ZoomPreset_Click" Tag="5.0"
                        Padding="6,4" FontSize="11" ToolTipService.ToolTip="Zoom to 500%"/>
                <Button Content="1000%" Click="ZoomPreset_Click" Tag="10.0"
                        Padding="6,4" FontSize="11" ToolTipService.ToolTip="Zoom to 1000%"/>
                <Button Content="2000%" Click="ZoomPreset_Click" Tag="20.0"
                        Padding="6,4" FontSize="11" ToolTipService.ToolTip="Zoom to 2000%"/>
                <Border Width="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" Margin="4,2"/>
                <Button Content="✕" Command="{x:Bind ViewModel.CloseCommand}"
                        ToolTipService.ToolTip="Close Zoom Panel (Esc)"
                        HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>
        <!-- Diagram display -->
        <WebView2 x:Name="ZoomBrowser" Grid.Row="1"/>
    </Grid>
</UserControl>
