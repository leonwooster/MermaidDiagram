<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MermaidDiagramApp.Views.ShapeToolbox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MermaidDiagramApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:MermaidDiagramApp.Models.Canvas"
    xmlns:vm="using:MermaidDiagramApp.ViewModels"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Toolbox" 
                   Style="{ThemeResource TitleTextBlockStyle}" 
                   Margin="12,12,12,0"/>

        <!-- Tab View -->
        <TabView Grid.Row="1" Margin="12,8,12,0" x:Name="ToolboxTabView">
            <TabView.TabItems>
                <TabViewItem Header="Shapes" IsClosable="False">
                    <Grid Padding="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Search Box -->
                        <AutoSuggestBox Grid.Row="0"
                                        PlaceholderText="Search shapes..."
                                        Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Margin="0,0,0,12"/>

                        <!-- Shape Categories -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:ShapeCategory">
                        <Expander Header="{x:Bind Name, Mode=OneWay}"
                                  IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}"
                                  HorizontalAlignment="Stretch"
                                  HorizontalContentAlignment="Stretch"
                                  Margin="0,0,0,8">
                            <ItemsControl ItemsSource="{x:Bind Shapes, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="canvas:ShapeTemplate">
                                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                Padding="8"
                                                Margin="0,4"
                                                CanDrag="True"
                                                DragStarting="Shape_DragStarting"
                                                Tag="{x:Bind}">
                                            <Grid ColumnDefinitions="Auto,*">
                                                <FontIcon Grid.Column="0"
                                                          Glyph="{x:Bind IconGlyph}"
                                                          FontSize="24"
                                                          Margin="0,0,12,0"/>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{x:Bind Name}"
                                                               FontWeight="SemiBold"/>
                                                    <TextBlock Text="{x:Bind Description}"
                                                               FontSize="12"
                                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                               TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
                    </Grid>
                </TabViewItem>
                
                <TabViewItem Header="Code" IsClosable="False">
                    <Grid Padding="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" 
                                   Text="Generated Mermaid Code" 
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Margin="0,0,0,8"/>
                        
                        <ScrollViewer Grid.Row="1" 
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto">
                            <TextBox x:Name="GeneratedCodeTextBox"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     TextWrapping="NoWrap"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}"
                                     BorderThickness="1"
                                     Padding="8"/>
                        </ScrollViewer>
                        
                        <StackPanel Grid.Row="2" 
                                    Orientation="Horizontal" 
                                    Spacing="8" 
                                    Margin="0,12,0,0">
                            <Button Content="Copy Code" 
                                    Click="CopyCode_Click"/>
                            <Button Content="Apply to Editor" 
                                    Click="ApplyToEditor_Click"/>
                        </StackPanel>
                    </Grid>
                </TabViewItem>
            </TabView.TabItems>
        </TabView>
    </Grid>
</UserControl>
